Explanation:
df.pivot(index='month', columns='city', values='temperature'): This creates a pivot table where:
The month is used as the index.
The city names become columns.
The temperatures are the values in the table.
This approach is both efficient and straightforward. Well done!